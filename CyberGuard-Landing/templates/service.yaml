apiVersion: v1
kind: Service
metadata:
  name: {{ include "CyberGuard-Landing.fullname" . }}
  labels:
    {{- include "CyberGuard-Landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "CyberGuard-Landing.selectorLabels" . | nindent 4 }}
